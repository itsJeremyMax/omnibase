{
  "name": "omnibase-mcp",
  "version": "0.1.28",
  "description": "Universal database MCP server for AI agents",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/itsJeremyMax/omnibase.git"
  },
  "homepage": "https://github.com/itsJeremyMax/omnibase",
  "bugs": "https://github.com/itsJeremyMax/omnibase/issues",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "main": "dist/src/index.js",
  "bin": {
    "omnibase-mcp": "dist/src/index.js"
  },
  "scripts": {
    "build": "tsc",
    "build:all": "tsc && pnpm run build:sidecar",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "test:docker:up": "docker compose -f tests-integration/docker-compose.yml up -d --wait",
    "test:docker:down": "docker compose -f tests-integration/docker-compose.yml down -v",
    "build:sidecar": "cd sidecar && mkdir -p bin && go build -ldflags=\"-s -w\" -o bin/omnibase-sidecar .",
    "start": "node dist/src/index.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "tsc --noEmit",
    "prepare": "node -e \"if (!process.env.CI) require('child_process').execSync('husky', {stdio:'inherit'})\"",
    "postinstall": "node scripts/postinstall.js"
  },
  "lint-staged": {
    "*.{ts,js,json,yaml,yml}": "prettier --write"
  },
  "keywords": [
    "mcp",
    "database",
    "sql",
    "agent",
    "ai",
    "usql",
    "postgres",
    "mysql",
    "sqlite"
  ],
  "author": "Jeremy Max",
  "dependencies": {
    "@clack/prompts": "^1.1.0",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "cli-table3": "^0.6.5",
    "node-sql-parser": "^5.4.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.8.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/node": "^25.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
